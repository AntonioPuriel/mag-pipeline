process {
    executor = 'slurm'
    queue    = 'CHANGE_ME'   // partition name on your cluster

    withLabel: 'process_high_memory' {
        queue = 'CHANGE_ME'  // large-memory partition (GTDB-Tk needs >100 GB)
    }
}

executor {
    queueSize = 20
}
